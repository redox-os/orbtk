pub use self::mdl2_assets_font::*;

/// Icons provided in the `MDL2 Assets font` are accessible via
/// their unique codepoints, representing the `glyph`. The codepoints
/// are mapped to this given constants.
pub mod mdl2_assets_font;
